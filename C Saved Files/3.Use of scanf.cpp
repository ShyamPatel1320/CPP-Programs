#include<stdio.h>
int main()
{
	int a;
		printf("Enter Value");
		scanf("%d",&a); // scanf() is concerned with only the address of the variable.It asks the user for its value. 
		printf("\nValue is %d",a); // \n prints the succeding sentence in a new line
}
